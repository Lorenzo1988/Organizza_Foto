# requirements.in — dipendenze dirette del progetto
# Genera requirements.txt con: pip-compile --generate-hashes requirements.in
# Installa con:                 pip install -r requirements.txt --require-hashes

Pillow>=10.0.0
imagehash>=4.3.1
python-dotenv>=1.0.0
piexif>=1.1.3
send2trash>=1.8.2
keyring>=25.0.0
bcrypt>=4.0.0
pip-audit>=2.7.0
pip-tools>=7.0.0
pytest>=8.0.0
